using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * This script will not include any of the requirements for the project, but will allow the camera to follow the current player
 */

public class CameraFollowPlayer : MonoBehaviour
{
    GameObject player;

    // Start is called before the first frame update
    void Start()
    {
        //Start game following the current player
        GetPlayerObject();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void GetPlayerObject()
    {
        
    }
}
